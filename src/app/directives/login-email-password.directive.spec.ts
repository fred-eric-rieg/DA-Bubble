import { LoginEmailPasswordDirective } from './login-email-password.directive';

describe('LoginEmailPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginEmailPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
